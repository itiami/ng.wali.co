import { PercentageCalcPipe } from './percentage-calc.pipe';

describe('PercentageCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
