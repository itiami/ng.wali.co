import { HighlightOperatorsDirective } from './highlight-operators.directive';

describe('HighlightOperatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightOperatorsDirective();
    expect(directive).toBeTruthy();
  });
});
